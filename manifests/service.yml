apiVersion: v1
kind: Service
metadata:
  name: kubenews
  labels:
    app: kubenews
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: kubenews
